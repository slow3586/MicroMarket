apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: orderservicedb-statefulset
spec:
  serviceName: orderservicedb-service
  replicas: 1
  minReadySeconds: 120
  selector:
    matchLabels:
      name: orderservicedb
  template:
    metadata:
      labels:
        name: orderservicedb
    spec:
      containers:
        - name: orderservicedb-container
          image: 'postgres:14'
          envFrom:
            - secretRef:
                name: dbsecret
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: orderservicedb-volume
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: orderservicedb-volume
      spec:
        storageClassName: orderservicedb-volume
        accessModes: [ "ReadWriteMany" ]
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: orderservicedb-service
spec:
  type: NodePort
  selector:
    name: orderservicedb
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
---
kind: PersistentVolume
apiVersion: v1
metadata:
  name: orderservicedb-volume
spec:
  storageClassName: orderservicedb-volume
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/micromarket/orderservicedb"